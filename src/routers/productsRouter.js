import express from 'express';

const router = express.Router();

//GET
router.get();
//GET id
router.get();
//POST
router.post();
//PATCH id
router.patch();
//DELETE id
router.delete();

export default router;
